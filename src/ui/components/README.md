# `ui/components`

Reusable, presentational components shared across screens (buttons, inputs,
cards, list items). Stateless where possible; they receive data and callbacks via
props. Design-system primitives live here — see `skills/design-system.md`.
